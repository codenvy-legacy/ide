<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.exoplatform.ide</groupId>
        <artifactId>exo-ide-parent</artifactId>
        <version>2.4.0-SNAPSHOT</version>
    </parent>
    <groupId>org.exoplatform.ide</groupId>
    <artifactId>exo-ide-gadget-server</artifactId>
    <packaging>war</packaging>
    <name>Codenvy IDE :: Extension :: Gadget</name>
    <dependencies>
        <dependency>
            <groupId>org.apache.shindig</groupId>
            <artifactId>shindig-server</artifactId>
            <version>${shindig.version}</version>
            <type>war</type>
        </dependency>
    </dependencies>
    <build>
        <sourceDirectory>src/main/resources</sourceDirectory>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-dependencies-war</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <includeTypes>war</includeTypes>
                            <silent>true</silent>
                            <stripVersion>true</stripVersion>
                            <outputDirectory>${project.build.directory}/packager-conf/war</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>build-gadget-server</id>
                        <phase>prepare-package</phase>
                        <configuration>
                            <tasks>
                                <unzip src="${project.build.directory}/packager-conf/war/shindig-server.war" dest="${project.build.directory}/packager-conf/unwar/gadget-server" />
                                <copy todir="${project.build.directory}/packager-conf/unwar/gadget-server/samplecontainer" overwrite="true">
                                    <fileset dir="${basedir}/src/main/resources/containers/samplecontainer" />
                                </copy>
                                <copy todir="${project.build.directory}/packager-conf/unwar/gadget-server/WEB-INF" overwrite="true">
                                    <fileset dir="${basedir}/src/main/webapp" />
                                </copy>
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <configuration>
                    <failOnMissingWebXml>true</failOnMissingWebXml>
                    <warSourceDirectory>${project.build.directory}/packager-conf/unwar/gadget-server</warSourceDirectory>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
