<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
   <modelVersion>4.0.0</modelVersion>
   <parent>
      <groupId>org.exoplatform.ide</groupId>
      <artifactId>exo-ide-parent</artifactId>
      <version>1.2.0-M3-SNAPSHOT</version>
   </parent>
   <artifactId>exo-ide-packaging-shell-war</artifactId>
   <packaging>war</packaging>
   <name>eXo IDE :: Packaging : Shell</name>

   <dependencies>
       <dependency>
         <groupId>org.exoplatform.ide</groupId>
         <artifactId>exo-ide-shell</artifactId>
         <version>${project.version}</version>
       </dependency>
        <dependency>
         <groupId>org.exoplatform.ide</groupId>
         <artifactId>exo-ide-editor</artifactId>
         <scope>provided</scope>
      </dependency>
      
   </dependencies>
   <build>
      <resources>
         <resource>
            <directory>src/main/resources</directory>
            <excludes>
               <exclude>**/*</exclude>
            </excludes>
         </resource>
      </resources>
      <plugins>
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
               <skipTests>true</skipTests>
            </configuration>
         </plugin>
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <executions>
               <execution>
                  <id>unwar-shell</id>
                  <phase>process-resources</phase>
                  <configuration>
                     <tasks>
                        <unwar src="${project.build.directory}/dependency/exo-ide-shell-${project.version}.jar" dest="${project.build.directory}/share" />
                     </tasks>
                  </configuration>
                  <goals>
                     <goal>run</goal>
                  </goals>
               </execution>
               <execution>
                  <id>move-compile-resources</id>
                  <phase>prepare-package</phase>
                  <configuration>
                     <tasks>
                        <move todir="${project.build.directory}/${project.artifactId}-${project.version}">
                           <fileset dir="${project.build.directory}/${project.artifactId}-${project.version}/Shell" />
                        </move>
                     </tasks>
                  </configuration>
                  <goals>
                     <goal>run</goal>
                  </goals>
               </execution>
            </executions>
         </plugin>
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-dependency-plugin</artifactId>
            <executions>
               <execution>
                  <id>copy-dependencies</id>
                  <phase>generate-sources</phase>
                  <goals>
                     <goal>copy</goal>
                  </goals>
                  <configuration>
                     <artifactItems>
                        <artifactItem>
                           <groupId>org.exoplatform.ide</groupId>
                           <artifactId>exo-ide-shell</artifactId>
                           <type>jar</type>
                        </artifactItem>
                     </artifactItems>
                  </configuration>
               </execution>
            </executions>
         </plugin>
         <!-- configure the GWT-Maven plugin -->
         <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>gwt-maven-plugin</artifactId>
            <version>${gwt.maven.plugin.version}</version>
            <configuration>
               <module>org.exoplatform.ide.shell.Shell</module>
               <runTarget>org.exoplatform.ide.shell.Shell/Shell.html</runTarget>
               <logLevel>INFO</logLevel>
               <skipTests>${gwt.test.skip}</skipTests>
            </configuration>
            <executions>
               <execution>
                  <phase>compile</phase>
                  <goals>
                     <goal>compile</goal>
                  </goals>
               </execution>
            </executions>
         </plugin>
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-war-plugin</artifactId>
            <configuration>
               <webXml>src/main/resources/WEB-INF/web.xml</webXml>
               <warSourceExcludes>.gwt-tmp/**,**/gadget/**,**/shell/**,**/application/**</warSourceExcludes>
               <packagingExcludes>**/classes/org/**,**/classes/gadget/**,**/Commons.css,
                  **/hosted.html,**/applicationInitializer.js,**/init_preload_gadget.js,
                  **/IDEShell.html,**/IDEGadget.html,**/ShowCase.html,**/preload.html,
                  **/preloadIDE.js,**/init_smartgwt_for_gadget.js,
                  **/exo-gwtframework-commons*.jar,**/exo-gwtframework-ui*.jar,**/exo-ide-*-client*.jar,**/exo.portal*.jar,
                  **/chromattic*tests.jar,**/servlet-api*.jar,**/exo-ide-client*.jar,**/portlet-api*.jar,**/jsf-api*.jar
                  </packagingExcludes>
                <packagingExcludes>WEB-INF/lib/*.jar</packagingExcludes>
               <webResources>
                  <resource>
                     <directory>src/main/resources</directory>
                     <targetPath>.</targetPath>
                  </resource>
               </webResources>
            </configuration>
         </plugin>
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <configuration>
               <source>${maven.compiler.source}</source>
               <target>${maven.compiler.target}</target>
               <excludes>
                  <exclude>**/IDEGadget.java</exclude>
               </excludes>
            </configuration>
         </plugin>
      </plugins>
      <sourceDirectory>target/share</sourceDirectory>
      <!-- outputDirectory>target/war/classes</outputDirectory -->
   </build>
</project>
